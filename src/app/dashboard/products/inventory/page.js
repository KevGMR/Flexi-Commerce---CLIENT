import { Placeholder } from "@/components/Placeholder";

export default function InventoryPage() {
  return (
    <Placeholder
      title="Inventory"
      description="View inventory levels. Use GET /inventory with location, variant, and lowStock filters."
    />
  );
}
