import { Placeholder } from "@/components/Placeholder";

export default function PurchaseOrdersPage() {
  return (
    <Placeholder
      title="Purchase Orders"
      description="Track procurement. Use GET /purchase-orders and related actions (send, confirm, receive, cancel)."
    />
  );
}
